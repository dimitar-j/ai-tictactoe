# About
An AI written in Python which utilizes the Minimax algorithm to play tic-tac-toe against the user intelligently.

# Getting Started
In the root directory run `pip3 install -r requirements.txt` and then `python runner.py` to run the game
